[package]
name = "arkyv-engine-server"
version = "0.1.0"
edition = "2021"
description = "SpacetimeDB module for Arkyv Engine"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = "=2.0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

